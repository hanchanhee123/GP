<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gamepiece.admin.event.mapper.EventMapper">
<select id="getEventList" parameterType="string" resultType="gamepiece.admin.event.domain.Event">
SELECT
	*
FROM
	events;

</select>
<select id="getEventParticipant" parameterType="string" resultType="gamepiece.admin.event.domain.Event">
SELECT
	id
FROM
	event_participations
WHERE
	ev_cd = #{ev_cd};
</select>
<select id="getEventDetail" parameterType="string" resultType="gamepiece.admin.event.domain.Event">
SELECT
	*
FROM
	events
WHERE
	ev_cd=#{ev_cd};
</select>
</mapper>